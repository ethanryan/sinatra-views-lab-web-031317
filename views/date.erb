<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%= "Today" %><h1>
      <% date = DateTime.now %>
      <% date.strftime("%A, %B %d, %Y") %>
      <!-- %A == full weekkday name, %B == full month name, %d == numerical day of month, %Y == year") %> -->
    <p><%= "The date is #{date.strftime("%A, %B %d, %Y")}" %><p>
  </body>
</html>
